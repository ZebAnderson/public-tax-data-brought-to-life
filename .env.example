# Supabase Postgres connection string (use the direct or pooler URL with password)
# Example:
# DATABASE_URL=postgresql://postgres:<PASSWORD>@db.<project-ref>.supabase.co:5432/postgres
DATABASE_URL=

# Which pilot dataset to ingest (defaults to "minneapolis")
TAXATLAS_PILOT=minneapolis

# Optional: override TIGER vintage year (defaults per pilot config)
TIGER_YEAR=

